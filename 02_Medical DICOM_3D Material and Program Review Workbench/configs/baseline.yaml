# All relative paths are resolved against the project directory, not the shell cwd.
data_root: ../chest_xray
subset_path: configs/train_subset.csv
output_dir: outputs/baseline
audit_dir: outputs/audit
metadata_csv: null
model: resnet18
pretrained: true
pretrained_weights: null
image_size: 224
train_samples_per_class: 300
batch_size: 16
epochs: 3
learning_rate: 0.001
backbone_learning_rate: 0.0001
freeze_backbone: true
random_seed: 42
device: cpu
num_workers: 0
torch_threads: 4
